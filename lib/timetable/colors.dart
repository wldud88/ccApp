import 'package:flutter/material.dart';

/// 앱 전반에서 쓰는 색상 토큰
class CCColors {
  // 토글 / 텍스트
  static const toggleBg = Color(0xFFE5E7EB);
  static const toggleActiveBg = Color(0xFFFFFFFF);
  static const toggleActiveText = Color(0xFF111827);
  static const toggleInactiveText = Color(0xFF6B7280);
  static const infoText = Color(0xFF6B7280);

  // 기본
  static const black = Colors.black;
  static const white = Colors.white;

  // 보조
  static const line   = Color(0xFFE5E7EB);
  static const accent = Color(0xFFAED6F1); // 대표 연파랑
}
